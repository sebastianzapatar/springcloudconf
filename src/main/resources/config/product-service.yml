spring:
  datasource:
    url: jdbc:postgresql://localhost:5434/products
    username: postgres
    password: "123"
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update

server:
  port: 8082

app:
  message: "Hola desde productos-service"